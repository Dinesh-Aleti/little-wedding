pipeline {
    agent any

    stages {
        stage ('git-checkout-with-webhook') {
            steps {
                git 'https://github.com/Dinesh-Aleti/little-wedding.git'
            }
        }
    }
}
